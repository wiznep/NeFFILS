import 'package:flutter/material.dart';

class AddIndustryManagementScreen extends StatelessWidget {
  const AddIndustryManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Industry Management'),
      ),
      body: Center(
        child: Text('Add Industry Management Screen'),
      ),
    );
  }
}